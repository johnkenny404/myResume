const displayName = function (fulname){
    const printName = `my name is ${fulname}`;
    return printName;
}

console.log(displayName('jack jade'));

document.write(displayName('Salihu Musa John'));
